node_modules/
dist/
*.local
.env
.env.*
!.env.example
.DS_Store
.superpowers/
.agents/
.claude/
skills-lock.json

# QA login credentials — never commit
scripts/qa-accounts.local

# Written by `npm run functions:check` (deno check); the deploy bundler resolves npm: specifiers itself.
deno.lock
